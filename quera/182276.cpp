#include <bits/stdc++.h>

using namespace std;

#define print(x) cout << #x << ": " << x << endl;

int main()
{
    string s; cin >> s;
    cout << (s == "EAST" ? "YES" : "NO") << endl;
    return 0;
}